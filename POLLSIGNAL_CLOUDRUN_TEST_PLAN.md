# pollSignal Cloud Run 이전 — 검증 테스트 계획 (2026-08-18, 계획 단계 — 코드 변경 없음)

이 문서는 `FEED_NOTIFICATIONS_CLOUDRUN_ANALYSIS.md`에서 1차 이전 후보로 정한 `pollSignal`을 실제로 옮기기 **전에**, 기존 Apps Script와 결과가 100% 동일한지 확인할 방법을 정리한 것입니다. **이 문서 작성 시점까지 Cloud Run 코드·Apps Script 코드는 하나도 바꾸지 않았고, 배포도 하지 않았습니다.** 아래 계획에 대해 승인받은 뒤에만 실제 코드 작업을 시작합니다.

## 1. 기존 Apps Script `pollSignal` 로직 정확히 확인한 결과

`pollSignal`(`handlePollSignal_`)은 아래 순서로 동작합니다.

1. `게시물`, `품목마스터`, `댓글` 시트를 전부 읽어옵니다.
2. 댓글을 게시물별로 미리 묶어두고(성능 최적화), 각 댓글 작성자의 소속팀도 미리 한 번씩만 조회해둡니다.
3. 게시물 하나하나에 대해 공용 함수 **`buildFeedEntry_`** 를 호출합니다. 이 함수가 핵심 로직 전체를 담당합니다:
   - **품목 매칭**: 이 게시물의 원자재명과 일치하고, 상태가 "활성"이며, 품목 등록일이 게시물 작성일보다 이전인 품목만 후보로 삼습니다(`getRelatedItems_`).
   - **열람 권한 필터링**(`canViewComment_`): 역할에 따라 다릅니다 — **임원**은 전체 팀 다 보임 / **팀장**은 설정 시트의 `팀장_열람범위` 값이 "전체"면 전체, 아니면 자기 팀만 / **담당·일반**은 항상 자기 팀만.
   - 이 필터를 통과하는 품목이 하나도 없으면 이 게시물 자체가 이 사용자에게는 "없는 것"으로 처리됩니다(`null` 반환, 신호에도 안 잡힘).
   - 남은 품목마다 "확인 여부"(`confirmed` = 댓글이 1개 이상 있는지), "댓글 수", "마지막 댓글 작성자·시각"을 계산합니다.
   - **답변 필요 여부(`needsAttention`)** 판단이 역할별로 다릅니다:
     - **담당**: 자기가 담당인 품목 중, (아직 확인 안 함) 또는 (마지막 댓글이 본인이 쓴 게 아니고, 마지막으로 알림함을 확인한 시각보다 나중에 달렸음) 이면 `true`.
     - **팀장·임원**: 보이는 품목 중, (아직 확인 안 함) 또는 (마지막 댓글 작성자가 본인이 아니고 마지막 확인 시각보다 나중) 이면 `true`.
     - **일반**: 이 두 분기에 해당 안 되므로 **항상 `false`** (일반 역할은 답변 필요 배지 자체가 없음).
4. 모든 게시물을 돈 뒤, `needsAttention === true`인 게시물 수를 `totalNeedsAttention`으로 합산합니다.
5. 각 게시물의 "보이는 품목"마다 `{postId, itemId, commentCount, lastCommentAt}` 딱 4개 필드만 뽑아서 `signatures` 배열에 넣습니다 (제목/요약 등 무거운 필드는 안 보냄 — 이게 `pollSignal`이 가벼운 이유).
6. 최종 응답: `{ ok: true, totalNeedsAttention, signatures }`.

**의존하는 다른 값들**: `user.role`, `user.team`, `user.name`, `user.email`, `user.lastCheckedAt`(사용자팀마스터 F열, `markChecked`가 기록) / 설정 시트의 `팀장_열람범위` 값.

## 2. 역할별 예상 결과 정리

| 역할 | `needsAttention` 판단 기준 | 참고 |
|---|---|---|
| 담당 | 본인이 담당인 품목 중 미확인, 또는 남이 쓴 새 댓글(마지막 확인 이후) | 본인 담당이 아닌 품목은 `isMine` 조건에서 걸러져 판단에서 제외 |
| 팀장 | 보이는 품목 중 미확인, 또는 남이 쓴 새 댓글 | `팀장_열람범위`가 "전체"면 다른 팀 품목도 포함, "자기팀"(기본값)이면 자기 팀만 |
| 임원 | 팀장과 동일한 기준, 단 팀 범위는 항상 전체 | — |
| 일반 | 항상 `false` | 보이는 품목 자체는 있을 수 있지만(자기 팀만) 배지 판단에는 안 들어감 |

`signatures`(품목별 댓글 수·마지막 댓글 시각)는 역할과 무관하게, **"보이는" 모든 품목**에 대해 나갑니다 — `needsAttention`인 품목만 나가는 게 아닙니다. 이 부분을 놓치면 실서비스에서 배지 숫자는 맞는데 하위 목록이 비거나 틀리게 나오는 버그가 생길 수 있습니다.

## 3. Cloud Run 이전 시 100% 동일성 검증 — 테스트 케이스

### 3-1. 검증 방식 (실제 사용자·실제 데이터를 건드리지 않는 방법)

`pollSignal`은 실제 게시물·품목·댓글(전부 실제 업무 데이터)을 계산 대상으로 삼기 때문에, 로그인 계정만 가짜로 만드는 지금까지의 방식(예: `qa-node22-upgrade-test@nkmro.invalid`)만으로는 **역할별 판단 분기(담당/팀장/임원/일반, `팀장_열람범위` 전체·자기팀)를 전부 실제로 통과시켜 볼 수 없습니다** — 그런 계정은 사용자팀마스터에 실제 행이 없어서 `USER_NOT_FOUND`로 끝나 버립니다.

그래서 이번엔 두 단계로 나눠서 검증할 것을 제안합니다.

- **1단계 — 로직 단위 비교(권장, 실제 데이터 변경 전혀 없음)**: 실제 게시물·품목·댓글 데이터를 **읽기만** 해서 스냅샷으로 뽑아두고, Apps Script 편집기에서 `buildFeedEntry_`/`handlePollSignal_`을 진짜 로그인·세션 없이 **직접 함수 호출**로 실행합니다 — `user` 값은 `{email, role, team, name, lastCheckedAt}` 형태로 아래 3-2의 시나리오대로 손으로 만들어 넣습니다(실제 계정도, 실제 세션도 필요 없음 — 시트에 아무것도 안 씀). 같은 스냅샷과 같은 `user` 값으로 Cloud Run(JS) 쪽 로직을 동일하게 만들어 돌린 뒤, 두 결과(JSON)를 한 글자도 다르지 않은지 비교합니다. **이 방식은 시트에 어떤 것도 새로 쓰지 않아 가장 안전합니다.**
- **2단계 — 실제 엔드포인트 통합 테스트(1단계 통과 후, 별도 승인 필요)**: 실제로 Cloud Run에 `pollSignalTest` 함수가 배포된 뒤, 진짜 세션 인증까지 포함해 끝에서 끝까지 확인하려면 사용자팀마스터에 **임시 합성 계정 행**(역할별로 담당/팀장/임원 각 1개, 상태는 "비활성" 등으로 명확히 표시)이 필요합니다. 이건 지금까지의 "실제 계정 손대지 않기" 원칙보다 한 단계 더 들어가는 것이라, **이 문서에서 미리 승인을 받는 게 아니라 1단계가 끝난 뒤 별도로 여쭤보겠습니다.**

지금 이 승인 범위(분석·테스트 계획)에서는 **1단계 방식만** 준비하고, 2단계는 나중에 별도로 여쭤봅니다.

### 3-2. 구체적 테스트 케이스 (1단계, 손으로 만든 `user` 값 × 실제 스냅샷 데이터)

| # | 시나리오 | 확인할 것 |
|---|---|---|
| 1 | 담당 역할, 본인 담당 품목 중 미확인 품목이 있음 | `needsAttention: true`, 해당 품목이 `signatures`에도 포함 |
| 2 | 담당 역할, 본인 담당 품목 전부 확인됨 + 마지막 댓글도 본인이 씀 | `needsAttention: false` |
| 3 | 담당 역할, 본인 담당 품목은 확인됐지만 팀장이 마지막 확인 시각 이후 새 답글을 남김 | `needsAttention: true` |
| 4 | 담당 역할, 본인 담당이 아닌 품목만 있는 게시물 | 그 품목이 `isMine` 조건에서 제외되어 `needsAttention` 판단에 영향 없음(다른 조건 없으면 `false`) |
| 5 | 팀장 역할, `팀장_열람범위 = 자기팀`(기본값), 다른 팀 품목이 미확인 상태 | 그 게시물이 `visibleItems`에서 완전히 제외(신호에도 안 잡힘) |
| 6 | 팀장 역할, `팀장_열람범위 = 전체`, 위와 동일한 다른 팀 미확인 품목 | 이번엔 보이고, `needsAttention: true` |
| 7 | 임원 역할, 여러 팀에 걸친 게시물들 | 팀 구분 없이 전부 보이고, 미확인/새 답글 기준으로 정확히 판단 |
| 8 | 일반 역할, 본인 팀에 미확인 품목이 있음 | 품목은 `signatures`에 보이되(팀 필터만 적용), `needsAttention`은 항상 `false` |
| 9 | `lastCheckedAt`이 없음(한 번도 알림함을 안 열어본 사용자) | 내부적으로 0으로 취급되어, 댓글이 하나라도 있으면 "마지막 확인 이후"로 판정 |
| 10 | 품목 등록일이 게시물 작성일보다 나중인 경우 | 그 품목은 `getRelatedItems_` 단계에서 후보에서 제외 |
| 11 | 게시물 여러 개, 역할별로 일부만 `needsAttention: true` | `totalNeedsAttention` 합계가 정확히 그 개수와 일치 |
| 12 | 댓글이 없는 품목(완전 미확인) | `commentCount: 0`, `lastCommentAt: null`, `confirmed: false` |

각 케이스마다 Apps Script 결과(JSON)와 Cloud Run 결과(JSON)를 나란히 저장해 **완전히 같은 값인지(문자 하나까지)** 비교하고, 다른 점이 있으면 그 원인을 찾아 고친 뒤 다시 비교합니다. 12개 전부 일치해야 다음 단계(2단계 통합 테스트)로 넘어갑니다.

## 4. 실제로 코드를 수정하게 된다면 예상되는 변경 범위 (지금은 수정하지 않음, 미리 보여드리는 것)

승인해 주시면 그때 진행할 코드 작업의 범위는 대략 이렇습니다.

- **`cloud-run/mro-functions/index.js`**: `pollSignalTest`라는 새 함수 추가(기존 `getSettingsTest`와 같은 패턴 — Firestore 세션 확인 → `touchSession_`로 슬라이딩 → 시트 읽기). 다만 이번엔 시트를 5개(게시물/품목마스터/댓글/설정/사용자팀마스터) 읽어야 하고, `getRelatedItems_`·`canViewComment_`·`buildFeedEntry_`와 동일한 계산 로직을 JS로 그대로 옮겨야 합니다. 사용자팀마스터를 읽는 범위도 지금(A~E열)보다 넓혀서 F열(마지막확인일시)까지 포함해야 합니다.
- **`feed.html`**: `CLOUD_RUN_POLLSIGNAL_URL` 상수 추가, `pollForUpdates()`가 이 상수가 채워져 있으면 Cloud Run을 먼저 시도하고 실패 시 기존 Apps Script 경로로 자동 폴백하도록 수정(`getSettings` 때와 동일한 안전 패턴).
- **건드리지 않는 것**: `getFeed`/`getNotifications`/`getPostById`/`markChecked`/`postComment` 등 다른 모든 action, Apps Script `Code.gs`(로직을 "읽어서 참고"만 하지 실제로 수정하지 않음).

이 문서는 계획까지입니다. 3-1의 1단계 로직 비교부터 시작해도 될지 확인해 주시면, 그때 스냅샷 준비와 비교 작업을 진행하겠습니다 (역시 코드 배포 없이, Apps Script 편집기에서의 읽기 전용 함수 호출 + 로컬 JS 비교로만 진행합니다).
