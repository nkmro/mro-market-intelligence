# 피드(getFeed)·알림(getNotifications) Cloud Run 이전 가능성 분석 (2026-08-18, 분석 단계 — 코드 변경 없음)

이 문서는 최상위 `README.md`의 API 매핑표에서 "시황 피드 조회·알림"이 "🔎 분석 진행 중"으로 표시된 항목에 대한 분석입니다. **이 문서는 분석만 한 것이며, 실제 코드는 하나도 바꾸지 않았습니다.** 실제 수정은 별도 승인을 받은 뒤에만 진행합니다.

## 1. 현재 상태 요약 (Code.gs 기준)

피드·알림 쪽은 겉보기엔 API가 여러 개(`getFeed`, `getNotifications`, `getPostById`, `pollSignal`)지만, 실제로는 전부 하나의 공용 함수 **`buildFeedEntry_`** 를 같이 씁니다. 게시물 하나당 "이 사용자에게 보여줄 품목 목록·확인 여부·답변 필요 여부(`needsAttention`)"를 계산하는 핵심 로직이 여기 다 들어 있고, 4개 API는 이 결과를 각자 다른 모양으로 잘라서 돌려줄 뿐입니다.

- **`getFeed`**: 메인 피드 화면. 전체 게시물을 계산한 뒤, "뉴스피드출력기간"(기본 14일) 이내이거나 아직 담당자가 확인 안 한 게시물만 최신순으로 페이지네이션해서 보여줍니다.
- **`getNotifications`**: 알림함. 역할별로 다르게 구성됩니다 — 담당소장은 "아직 최초 확인 댓글을 안 단 게시물", 팀장/임원은 "새 게시물 + 답글이 필요한 스레드".
- **`getPostById`**: 알림을 눌러서 특정 게시물로 바로 이동할 때 그 게시물 하나만 계산해서 돌려줍니다.
- **`pollSignal`**: 화면이 열려 있는 동안 **30초마다 자동으로 호출되는** 실시간 배지 갱신용 경량 API입니다. 무거운 제목/요약 등은 빼고 "이 품목에 댓글이 몇 개 있는지·언제 마지막으로 달렸는지"와 전체 답변 필요 개수만 가볍게 돌려줍니다.

**중요한 점: 이 4개는 모두 순수 읽기(조회) 작업입니다.** 시트에 아무것도 쓰지 않습니다 — 앞서 분석한 `postComment`(쓰기)와는 근본적으로 다른 상황입니다.

## 2. Cloud Run 이전에 유리한 점

- **시트 쓰기 권한 문제가 없습니다.** `postComment` 분석에서 나온 "Cloud Run은 읽기 전용이라 못 옮긴다"는 이유가 여기엔 적용되지 않습니다. `getTeams`/`getSettings`와 똑같은 조건(읽기만 하면 됨)입니다.
- **가장 자주 불리는 API가 여기 있습니다.** `pollSignal`은 화면이 열려 있는 내내 사용자 1명당 30초마다 호출됩니다. 실제 업무 시간 내내 여러 명이 동시에 접속해 있다면, 이 API가 전체 트래픽의 상당 부분을 차지할 가능성이 높습니다. 즉 **성능 개선 효과를 가장 크게 볼 수 있는 후보**입니다.

## 3. 그래도 신중해야 하는 이유

- **로직이 이 시스템에서 가장 촘촘하고 복잡한 영역입니다.** `buildFeedEntry_` 하나에 "역할별(담당/팀장/임원) 답변 필요 여부 판단", "팀별 열람 권한 필터링", "품목별 확인 여부·마지막 댓글 계산" 등이 전부 들어 있습니다. `getTeams`/`getSettings`처럼 시트 한 줄 그대로 돌려주는 수준이 아니라서, Cloud Run(JS)으로 옮기면서 한 글자도 다르지 않게 재현해야 하는 부담이 `postComment`의 읽기 부분보다도 훨씬 큽니다.
- **4개 API가 로직을 공유하기 때문에, 하나만 반쪽으로 옮기면 오히려 위험합니다.** 예를 들어 `pollSignal`만 Cloud Run으로 옮기고 `getFeed`는 Apps Script에 남겨두면, 두 로직이 아주 조금이라도 다르게 동작하는 순간 "배지에는 답변 필요라고 나오는데 피드에는 안 보인다" 같은 눈에 보이는 불일치가 생길 수 있습니다. 그래서 이 영역은 **하나씩 따로 옮기기보다, 공용 로직(`buildFeedEntry_`)을 한 번 제대로 포팅해서 4개가 전부 그걸 재사용하는 방식**이 안전합니다.
- **사용자 정보 조회 범위를 넓혀야 합니다.** 지금 Cloud Run의 `whoamiTest`는 사용자팀마스터 시트에서 이메일·이름·역할·팀·상태(A~E열)까지만 읽어 옵니다. `needsAttention` 판단에는 "마지막확인일시"(F열, `markChecked`가 기록하는 값)도 필요한데, 이건 아직 Cloud Run 쪽에서 읽어오지 않고 있습니다 — 범위를 넓히는 정도의 간단한 추가 작업이지만 빠뜨리면 답변 필요 배지가 틀리게 나옵니다.
- **`markChecked`(알림함 열람 시각 기록)는 쓰기 작업이라 이번에도 Apps Script에 남습니다.** `getFeed`/`getNotifications`가 Cloud Run으로 가더라도, "마지막확인일시"를 기록하는 `markChecked`는 `postComment`처럼 쓰기이므로 그대로 Apps Script에 두면 됩니다(`getSettings`가 Cloud Run으로 가도 `updateSettings`는 Apps Script에 남아있는 것과 같은 패턴).

## 4. 결론 및 권장 진행 순서

**구조적으로 막혀 있지는 않습니다** (`postComment`와 달리 쓰기 권한 문제가 없음) — 이전 자체는 가능합니다. 다만 이 시스템에서 로직이 가장 복잡하고, 4개 API가 서로 얽혀 있어서 한 번에 다 옮기기보다 단계적으로 접근하는 것을 권장합니다.

1. **1단계(제안)**: 가장 자주 호출되고 응답이 가벼운 `pollSignal`부터 시범적으로 Cloud Run에 옮기고, 담당/팀장/임원 역할별로 합성 테스트 계정을 만들어 Apps Script 버전과 결과가 완전히 똑같은지(특히 `needsAttention`, `totalNeedsAttention` 값) 충분히 검증합니다.
2. **2단계(제안)**: 1단계가 문제없이 안정화되면, 같은 포팅된 로직을 재사용해 `getFeed`·`getNotifications`·`getPostById`도 순서대로 옮깁니다.
3. **어느 단계든 실패 시 자동으로 Apps Script로 폴백**하는 지금까지의 안전 패턴(상수를 비우면 즉시 롤백)을 그대로 적용합니다.
4. `markChecked`는 이번 이전 범위에 포함하지 않고 계속 Apps Script에 둡니다.

이번 분석은 여기까지이며, **위 1·2단계 진행 여부는 별도 승인을 받은 뒤에 실제 계획서(상세 구현안)를 다시 작성해 진행하겠습니다.** 최종 판단은 실제 업무 담당자가 하는 것이 안전합니다.
