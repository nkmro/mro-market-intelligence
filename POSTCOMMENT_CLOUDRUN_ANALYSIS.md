# postComment(댓글) 기능 Cloud Run 이전 가능성 분석 (2026-08-18, 분석 단계 — 코드 변경 없음)

이 문서는 최상위 `README.md`의 API 매핑표에서 `postComment` 등 댓글 관련 action이 "⏳ 미착수"로 표시된 항목에 대해, `getTeams`/`getSettings`/`login·whoami`와 같은 방식으로 Cloud Run으로 옮길 수 있는지 살펴본 결과입니다. **지금 이 문서는 분석만 한 것이며, 실제 코드는 하나도 바꾸지 않았습니다.**

## 1. 현재 댓글 기능이 하는 일 (Code.gs 기준)

댓글 관련 action은 4개입니다: `getComments`(조회), `postComment`(작성), `updateComment`(수정), `deleteComment`(삭제).

- **조회(`getComments`, `handleGetComments_`)**: 특정 게시물의 댓글을 모두 가져온 뒤, 보는 사람의 역할(임원/팀장/담당/일반)과 팀에 따라 서버에서 걸러서 보여줍니다. 순수 읽기 작업입니다.
- **작성(`postComment`, `handlePostComment_`)**:
  - 역할이 "일반"이면 아예 작성 금지.
  - 품목(itemId)에 대한 **첫 댓글**은 그 품목의 **담당소장만** 남길 수 있고(다른 사람이 먼저 쓰면 안 됨), 이미 첫 댓글이 있으면 그다음부터는 담당소장/팀장/임원이 자유롭게 답장을 남길 수 있습니다.
  - 답장(parentCommentId)을 달 때는 그 댓글이 실제로 존재하는지 확인합니다.
  - 시트(`댓글` 시트)에 새 행을 **추가(쓰기)** 합니다.
  - 등록 직후 프론트가 다시 조회하지 않도록, 갱신된 댓글 목록과 해당 게시물의 최신 피드 정보를 함께 돌려줍니다 (요청 3번 → 1번으로 줄이는 성능 최적화).
  - **중복 등록 방지(idempotency)**: 같은 요청이 네트워크 문제로 재시도되어도 댓글이 두 번 등록되지 않도록, Apps Script의 `CacheService`(임시 저장소)와 `LockService`(동시 실행 잠금)를 함께 써서 막고 있습니다.
- **수정(`updateComment`)/삭제(`deleteComment`)**: 본인이 쓴 댓글만 수정·삭제 가능. 역시 시트에 **쓰기** 작업이 들어갑니다.

프론트(`feed.html`)도 화면에 "등록 중..." 표시를 먼저 보여주고 실제 서버 응답이 오면 정리하는 방식(낙관적 업데이트)으로 맞물려 있어서, 서버 쪽 응답 형태를 한 글자도 다르지 않게 유지해야 화면이 깨지지 않습니다.

## 2. Cloud Run으로 옮기려면 걸리는 것들 (지금 당장 못 옮기는 이유)

- **가장 큰 문제 — 쓰기 권한 없음**: 지금 Cloud Run 함수 8개는 전부 **Google Sheets를 읽기만 할 수 있는 권한**으로 설정돼 있습니다 (`LOGIN_WHOAMI_MIGRATION_PLAN.md`에도 같은 내용이 있습니다). `postComment`/`updateComment`/`deleteComment`는 전부 시트에 **새로 쓰거나 고치는** 작업이라, 지금 상태로는 애초에 실행이 안 됩니다. Cloud Run 서비스 계정에 쓰기 권한을 새로 부여해야 하는데, 이건 "읽기 전용"이라는 지금까지의 안전한 전제를 깨는 큰 결정이라 신중해야 합니다.
- **중복 등록 방지 장치가 Apps Script에만 있음**: 위에서 설명한 `LockService`(동시 실행 막기)는 Google Apps Script에만 있는 기능이라 Cloud Run(Node.js)에는 그대로 옮길 수 없습니다. 다른 방식(Firestore를 이용한 잠금 등)으로 새로 만들어야 하는데, 잘못 만들면 "새로고침하다가 댓글이 두 번 등록되는" 것 같은 실제 사용자에게 보이는 버그로 이어질 수 있습니다.
- **세션의 "진짜 주인"이 아직 Apps Script**: `login/whoami` 계획에서 이미 설명했듯, 로그인 세션을 최종적으로 책임지는 곳은 아직 Apps Script(CacheService)입니다. 댓글 작성처럼 "잘못되면 실제 데이터가 바뀌는" 작업을 세션 검증이 완전히 자리잡기 전에 옮기는 것은 위험도가 높습니다.
- **로직이 복잡하고 촘촘함**: "첫 댓글은 담당소장만" 같은 권한 규칙, 답장 유효성 검사, 팀별 열람 권한 필터링, 게시물 요약 정보 재계산까지 한 번의 요청 안에서 다 처리합니다. 이걸 Cloud Run(JS)으로 그대로 옮기면서 한 글자도 다르지 않게 동작하도록 만드는 건, 지금까지 옮긴 `getTeams`/`getSettings`(단순 조회)보다 훨씬 손이 많이 가고 실수 여지도 큽니다.

## 3. 결론 및 권장사항

**`postComment`/`updateComment`/`deleteComment`는 지금 당장 Cloud Run으로 옮기지 않는 것을 권장합니다.** 이유는 위 2번의 "쓰기 권한 없음"이라는 구조적인 문제 하나만으로도 충분합니다 — 권한을 새로 열어주는 것부터가 이미 별도의 신중한 결정이고, 설령 권한을 연다 해도 중복 등록 방지·세션 신뢰 문제가 함께 해결돼야 실사용에 안전합니다.

다만 조사하면서 한 가지 괜찮은 대안을 찾았습니다.

- **`getComments`(댓글 조회)는 순수 읽기 작업이라 `getTeams`/`getSettings`와 똑같은 방식으로 안전하게 옮길 수 있는 후보입니다.** 다만 지금 실제로 눈에 보이는 성능 문제(느림)가 있는지는 확인되지 않았고, 굳이 지금 급하게 옮겨야 할 이유도 없어 보입니다. 필요하다고 판단되시면 별도로 계획서를 만들어 진행할 수 있습니다.

정리하면: **쓰기가 들어가는 댓글 작성·수정·삭제는 보류**, **읽기만 하는 댓글 조회는 원하시면 다음 후보로 검토 가능**합니다. 최종 판단은 실제 업무 담당자가 하는 것이 안전합니다.
