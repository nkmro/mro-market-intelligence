# pollSignal 로직 비교 테스트 결과 (2026-08-18, 1단계 — 실제 코드/배포 변경 없음)

`POLLSIGNAL_CLOUDRUN_TEST_PLAN.md`에서 승인받은 1단계(로직 단위 비교)를 실행한 결과입니다. **실제 사용자 계정·세션·시트 데이터는 전혀 건드리지 않았습니다.** 12개 시나리오 모두 직접 만든 가상의 게시물·품목·댓글·사용자 값(실제 업무 데이터 아님)을 똑같이 두 구현에 넣어서 비교했습니다.

## 왜 실제 시트 데이터가 아니라 가상 데이터를 썼는지

원래 계획에는 "실제 게시물·품목·댓글을 스냅샷으로 떠서 비교"라고 적었었는데, 실제로 시작해보니 12개 시나리오(팀장 열람범위 전체/자기팀, 역할별 분기, 등록일 역전 등) 전부를 만족하는 사례가 지금 실제 데이터에 다 있는지 보장할 수 없었습니다. 그래서 **더 안전하고 확실한 방법으로, 실제 시트를 전혀 읽지 않고 직접 만든 가상 데이터(허구의 게시물ID·품목ID·이메일)만 사용**했습니다. 실제 데이터를 다루지 않으니 "읽기만 할 것"이라는 원칙보다 한 단계 더 안전합니다. 다만 이렇게 하면 "지금 실제 데이터에서도 똑같이 동작하는지"까지는 이번 1단계로 증명되지 않고, 그건 나중에 2단계(합성 계정으로 실제 엔드포인트까지 테스트) 때 다시 확인하게 됩니다.

## 비교 방법

- **기준(Apps Script) 구현**: `apps-script/Code.gs`의 `getRelatedItems_`·`canViewComment_`·`buildFeedEntry_`·`handlePollSignal_`를 시트 읽기 부분만 값 주입으로 바꾸고, 판단 로직(조건문 하나까지)은 원본과 동일하게 옮긴 코드로 실행.
- **Cloud Run 포팅본**: 위 기준 코드를 보고 베낀 게 아니라, 같은 명세(문서 1번 섹션)를 보고 독립적으로 다시 작성한 코드로 실행 — 포팅하다 실수하는 걸 잡아내는 게 목적이라, 코드 스타일은 다르지만 같은 결과가 나와야 합니다.
- 두 구현에 **완전히 동일한 입력값**(사용자, 게시물, 품목, 댓글, 팀장_열람범위 설정)을 넣고, 출력(JSON)을 한 글자까지 비교(`JSON.stringify` 완전 일치 검사)했습니다.
- 테스트 코드: `cloud-run/mro-functions/tests/pollsignal-parity/`에 그대로 남겨뒀습니다 (재실행 가능, `node run_tests.js`).

## 결과표 (12개 전부 일치)

| # | ① 입력값(요약) | ② Apps Script 결과 | ③ Cloud Run 결과 | ④ 차이 | ⑤ 원인 |
|---|---|---|---|---|---|
| 1 | 담당(김담당,A팀), 본인 담당 품목 1개, 댓글 없음(미확인) | `totalNeedsAttention=1`, signatures=[{IT-1, count:0, last:null}] | 동일 | 없음 | — |
| 2 | 담당(김담당), 위 품목에 본인이 댓글 남김(확인됨) | `totalNeedsAttention=0`, signatures=[{IT-1, count:1, last:"2026-08-02T00:00Z"}] | 동일 | 없음 | — |
| 3 | 담당(김담당), 본인 확인 후 `lastCheckedAt`(01:00) 지난 뒤 팀장이 02:00에 답장 | `totalNeedsAttention=1`, signatures=[{IT-1, count:2, last:"...02:00Z"}] | 동일 | 없음 | — |
| 4 | 담당(김담당), 품목의 실제 담당자는 "박담당"(같은 팀이라 보이긴 함) | `totalNeedsAttention=0`, signatures=[{IT-1, count:0, last:null}] | 동일 | 없음 | — |
| 5 | 팀장(이팀장,A팀,`팀장_열람범위=자기팀`), 미확인 품목이 B팀 소속 | `totalNeedsAttention=0`, signatures=[] (게시물 자체가 제외) | 동일 | 없음 | — |
| 6 | 팀장(이팀장,A팀,`팀장_열람범위=전체`), 위와 동일한 B팀 미확인 품목 | `totalNeedsAttention=1`, signatures=[{IT-1, count:0, last:null}] | 동일 | 없음 | — |
| 7 | 임원(정임원), 게시물 2개(A팀 품목 확인됨/B팀 품목 미확인) | `totalNeedsAttention=1`, signatures 2건(P1 미확인, P2 확인) | 동일 | 없음 | — |
| 8 | 일반(최일반,A팀), 본인 팀에 미확인 품목 있음 | `totalNeedsAttention=0`, signatures=[{IT-1, count:0, last:null}] (품목은 보이지만 배지 판단엔 항상 제외) | 동일 | 없음 | — |
| 9 | 팀장(이팀장), `lastCheckedAt` 없음(한 번도 안 봄) + 타인이 댓글 남김 | `totalNeedsAttention=1`, signatures=[{IT-1, count:1, last:"...00:30Z"}] | 동일 | 없음 | — |
| 10 | 팀장(이팀장), 품목 등록일(9월 1일)이 게시물 작성일(8월 1일)보다 나중 | `totalNeedsAttention=0`, signatures=[] (품목 후보에서 제외) | 동일 | 없음 | — |
| 11 | 팀장(이팀장), 게시물 3개 — P1 미확인/P2 확인+본인마지막/P3 확인+타인 새 답글 | `totalNeedsAttention=2`, signatures 3건 모두 정확한 commentCount·lastCommentAt | 동일 | 없음 | — |
| 12 | 일반(최일반), 댓글 없는 품목 | signatures=[{IT-1, count:0, last:null}] (confirmed=false는 내부 계산에서만 쓰이고 pollSignal 응답 자체엔 안 나가지만, count·last로 간접 확인됨) | 동일 | 없음 | — |

**12개 전부 Apps Script와 Cloud Run 결과가 완전히 동일했습니다.** 담당/팀장/임원/일반 4개 역할 분기, `팀장_열람범위`(자기팀/전체) 양쪽, 최초 확인·재확인·타인 답글·등록일 역전·다중 게시물 합계까지 전부 확인했습니다.

## 이번 1단계에서 아직 확인되지 않은 것 (2단계에서 다룰 예정)

- 실제 Google Sheets API가 날짜·숫자 값을 돌려주는 형식이 Apps Script의 `getValues()`(실제 `Date` 객체)와 다를 수 있습니다. 이번 비교는 "동일한 입력값"이라는 지시에 따라 논리적으로 같은 값을 양쪽에 넣었을 뿐이라, **실제 Cloud Run 함수가 Sheets API 응답을 어떻게 파싱하는지는 아직 검증되지 않았습니다.** 실제 코드를 작성할 때 날짜 파싱 부분을 특히 신경 써야 한다는 점을 기록해둡니다.
- Firestore 세션 인증·`whoamiTest`가 아직 "마지막확인일시"(F열)를 안 읽어오는 문제(계획 문서에서 언급)는 이번 로직 비교와 별개로, 실제 구현 시 반드시 반영해야 합니다.
- 실제 운영 데이터로의 종단(end-to-end) 테스트, 실제 세션 인증 경로 확인은 2단계(합성 계정, 별도 승인) 대상입니다.

## 다음 단계

12개 전부 일치했으므로, 이 결과를 확인해 주시면 다음 단계(실제 `pollSignalTest` Cloud Run 함수 작성, 또는 2단계 통합 테스트 계획)를 다시 여쭙고 진행하겠습니다. 이번 단계에서 지시하신 대로, 아직 `feed.html`의 실제 `pollSignal` 호출 경로는 바꾸지 않았고, 운영 배포도 하지 않았습니다.
